/** Согласованы с токенами в `index.css` для читаемых графиков. */
export const CHART = {
  ok: '#1f8f4a',
  warn: '#c9970a',
  bad: '#c62828',
  /** План — холодный синий; факт — тёплый янтарь/медь: на графике линии не сливаются. */
  plan: '#164b91',
  fact: '#b45309',
  grid: '#e1e4ea',
  axis: '#6b7280',
  tooltipBg: '#ffffff',
  tooltipBorder: '#e1e4ea',
} as const
